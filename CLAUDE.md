# Лис Микита — бот редакції МикВісті

Внутрішній Telegram-бот редакції новинного медіа МикВісті (nikvesti.com, Миколаїв).
Моніторить документи влади, тендери, конкурентів, правоохоронців, збирає аналітику, надсилає ранкові зведення.

**Репозиторій:** github.com/derenuga/nikvesti-bot  
**Хостинг:** Railway (проект `remarkable-stillness`)  
**Стек:** Python, python-telegram-bot 21.9, APScheduler, BeautifulSoup, Google Analytics Data API, Google Search Console API, Anthropic Claude (tool use), matplotlib  
**Бот:** @mykvisti_bot  
**Чат редакції:** -1001857099475  
**Канал "🦊 Микита винюхав":** -1004322862192 (тендери + документи влади + правоохоронці)

---

## Деплой

Railway автодеплоїть з гілки `main`. **Щоб зміна пішла в прод — вона має бути в `main`.**
Робота у веб-сесії ведеться на feature-гілці з PR, але це проміжний крок, а не фініш:
**після того як зміни готові й запушені — одразу мерджити PR (гілку) у `main`**, щоб
задеплоїлось. Не лишати готову роботу висіти в PR і не чекати окремого прохання
«закинь на main» — це стандартне дозволене завершення задачі. Виняток — якщо явно
попросили лишити на review.

---

## Правила роботи з кодом

1. **Повні файли завжди.** Ніколи не давати патчі, інструкції "додай після рядка X" або часткові фрагменти. Тільки повний готовий файл для заміни.
2. **Не зупинятись на питаннях.** Якщо є неясність — уточнити одне питання і продовжувати, не чекати підтвердження кожного кроку.
3. **Тестувати парсери на реальному HTML.** Не писати власні зразки HTML, не тестувати на вигаданих даних.
4. **Baseline моніторингу з тестовою відправкою.** При першому запуску модуля моніторингу зберегти всі ID крім N останніх як бачені, N останніх одразу відправити — щоб перевірити що відправка і формат працюють. N вказувати явно в коментарі з поясненням.
5. **Emoji напряму в коді**, не як Unicode escape sequences (`🦊`, не `\U0001F98A`).
6. **Перевіряти факти** — особливо структуру HTML і API перед написанням парсерів.

---

## Структура коду

```
bot.py                    — реєстрація handlers, команди, TypeHandler middleware
handlers/
  scheduler.py            — APScheduler, розклад всіх автозавдань (Europe/Kiev)
  storage.py              — JSON-стан на Railway Volume (/data/prozorro_state.json)
  db.py                   — тонкий read-only адаптер до MySQL-БД сайту (SELECT only, SSL), /dbtest, /dbquery
  bot_db.py               — власна Postgres-БД бота (Railway): дзеркало архіву, tsvector FTS, sync_state, daily_stats (історія трафіку GA4), social_stats (тижневі зрізи соцмереж), social_monthly (місячна історія всіх мереж), article_stats (снімки /stat + object_id для швидкого шляху); таблиці team_* (Mini App «Команда») створюються ліниво у своїх модулях, не тут. Модель — з'єднання на запит, АЛЕ для складених операцій є два контексти: session() (одне з'єднання на весь блок — конект коштує ~99% часу запиту, критично для Mini App із десятками запитів на екран) і transaction() (одне з'єднання + одна транзакція, «або все, або нічого»)
  archive_mirror.py       — синк дзеркала архіву з БД сайту: /archive_backfill (разово), інкремент щогодини :50; /articles_backfill — разовий долив історичних статей (type='article'), які головний бекфіл проминув зі старим фільтром (свій курсор articles_backfill_last_id, основні курсори не чіпає)
  archive_search.py       — повнотекстовий пошук по дзеркалу (17 років, заголовки+текст), NLQ-tool search_archive_fulltext
  dossier.py              — /dossier <тема>: історія питання з архіву, таймлайн по роках з лінками
  entity_layer.py         — сутнісний шар нори (entities/article_entities, крок C ENTITY_LAYER_PLAN): бэкфіл через Batch API з бота — /entity_estimate (безкоштовна оцінка), /entity_backfill (платно, Haiku 4.5 −50%, полінг у фоні, стан у sync_state переживає редеплой), /entity_status, /entity_resume, /entity_resync (перечит конкретних статей після підміни вмісту ноди — зі зняттям старих зв'язків); злиття — entity_pipeline.write_results (корінь репо), промпт витягу — entity_extract_prompt.md; відбір для бэкфіла (фільтр типу + пропуск уже розібраного) — entity_backfill_api.fetch_range, тест test_entity_backfill_filter.py. Авто-інкремент самозаліковний: відбирає статті за ФАКТОМ відсутності зв'язків (таблиця `entity_attempts` рахує спроби й тримає причину збою), тому дірка від упалого витягу затягується наступної години — курсорна схема її втрачала назавжди (інцидент 11.07–01.08.2026, 403 статті; тест test_entity_increment.py)
  entity_roles.py         — канон РОЛЕЙ сутнісного шару (друга вісь дедупу, ENTITY_MERGE_PLAN §3.1): одна посада живе десятками написань («мер Миколаєва» = «міський голова» = «очільник»), і це не дублі карток — роль лежить у article_entities.role_at_time поштучно на кожну статтю. Тому лікується довідником ПОРУЧ (role_canon + role_variants), а сирий role_at_time не переписується ніколи: він факт статті, показує, як людину називали ТОДІ. Детектор без AI (спільний носій у перетинні періоди +3, trgm-схожість, вкладеність написань, спільне рідкісне слово органу), рішення — ЛЮДИНА інлайн-кнопками (/roles_dedup); «ні, різні» пам'ятається назавжди (role_pairs). Після зведення бот другою клавіатурою питає орган → role_canon.org_entity_id, тобто афіліація людина↔організація, якої бракує банку тем (кандидати — спершу за назвою посади, бо співпояв ловить СЮЖЕТ: міністру юстиції він пропонував НАБУ й «Квартал 95»; виправити потім — /roles_org). Споживачі читають view v_entity_roles (сира роль + канон поруч; без канону канон = сира роль, тож JOIN однаковий до й після). Кандидати розкладаються на КЛАСИ (різні номери — ніколи не злиття · перестановка слів · скорочення · уточнення трьома купами: доповнює назву / інший регіон / розрізняє · одне слово замінено · друкарська різниця · спільний носій), і клас закривається одним рішенням у /roles_bulk — по одній парі 2109 кандидатів фізично не пройти. Відкат — мінус рядок довідника (/roles_forget), /roles, /roles_canon, /roles_rename. Нормалізація ролі живе двічі (SQL-функція role_norm + Python) і звіряється тестом на КОЖНОМУ написанні з нори
  entity_merge.py         — злиття КАРТОК: /entity_classes — розкладка кандидатів по класах обома осями (перестановка слів / скорочення / уточнення з показом зайвого слова / друкарська різниця / спільний носій): замір 02.08 дав 3757 пар по картках і 2091 по ролях, тобто черга «одна пара — одне питання» не тягне і питати треба класом; /entity_scale — read-only замір масштабу дублів (ENTITY_MERGE_PLAN §6, числа й висновки — §6.1) окремо по картках (схожість написання через trgm-індекс, картки-одноденки, однакове рос. написання при різному укр. — випадок «Кім/Ким») і окремо по ролях (сирих написань → після нормалізації, люди з >3 ролями, скільки пар знайшов би детектор); ПЛЮС журнал злиттів entity_merges — /entity_dedup видаляє програшну картку, тож без знімка помилку не відкотиш: пишемо картку, всі її зв'язки з role_at_time/salience і стан переможця, поверх якого лягло злиття (/entity_merge_log, /entity_unmerge). Автозлиття без людини немає і не планується — однофамільці злипаються незворотно
  builder_monitor.py      — монітор оновлення білдера головної (options + nodes з БД), /builder, /builder_test
  fb_missing.py           — монітор миколаївських новин без Facebook: щогодини (9–21) дивиться свіжі новини Миколаєва (nodes, region=1 — коди регіонів: 1 Миколаїв, 2 Україна, 3 Світ, 4 Херсон, 5 Одеса; до 31.07 фільтр був own_material=1; вікно [now−24год, now−3год]) і тими ж get_fb_stats, що /stat, перевіряє, чи є пост у ФБ; якщо ні — РАЗ підказує редакції (лінк + автор із TG-хендлом за TEAM) і дає готову чернетку поста окремим блоком коду (json-ld title + description + лінк). Гейт «час настав»: nodes.published бреше для запланованих в адмінці новин (буває минулим до фактичного виходу), тому перед алертом читає сторінку статті й звіряє JSON-LD datePublished — час не настав (+ грейс 3 год від реального виходу) або сторінка не читається → відкласти без марку до наступної години. Перший запуск і перший після зміни фільтра (baseline_ver) — тихий baseline. /fbmissing (прогнати зараз), /fbmissing_test (прев'ю формату)
  ai_messages.py          — AI-шар (Anthropic Claude), FOX_SYSTEM_PROMPT, TEAM словник
  morning.py              — ранкове повідомлення: погода + події міськради + AI текст
  events.py               — парсинг календаря подій mkrada.gov.ua/calendar/
  prozorro.py             — моніторинг тендерів Prozorro ≥1 млн грн
  documents.py            — моніторинг документів органів влади (міськрада, ОВА, облрада)
  law_enforcement.py      — моніторинг новин правоохоронних органів (прокуратура тощо)
  competitors.py          — моніторинг новин конкурентів (news.pn тощо)
  google_analytics.py     — GA4 щоденна аналітика, /analytics, /report
  analytics_store.py      — пам'ять щоденної аналітики GA4 у Postgres (daily_stats): тихий щоденний захват (capture_yesterday), /analytics_backfill, серія для NLQ-tool get_traffic_history; ПЛЮС денний розріз Search Console по типах пошуку (sc_daily_stats: web/discover/googleNews) — захват піггібеком о 09:00 (трейлінг-вікно, бо SC латентить), /sc_backfill, тоталі по каналах для NLQ-tool get_search_console_history («трафік без Discover» дешево з локальної БД). AI Overviews/AI Mode Google окремим типом в API НЕ віддає — вони в 'web'
  weekly_digest.py        — «Тижневик Лиса»: понеділковий дайджест тижня сайту з порівнянням тиждень-до-тижня (заміна щоденного 09:00-звіту), /weekly
  traffic_spikes.py       — детектор сплесків трафіку (GA4 Realtime, самонавчальний профіль), /traffic
  stat.py                 — /stat <url>: статистика матеріалу (Facebook + Instagram + TikTok + YouTube + Telegram + GA4). Повторні виклики — швидким шляхом по індексу article_stats (минають пошук/матчинг), збій живого джерела → фолбек-снімок «з Нори»
  stat_store.py           — снімки /stat у Нору (article_stats, upsert «останній стан»): збереження після кожного /stat, індекс object_id для швидкого шляху, mark_nora для фолбека
  stat_instagram.py       — пошук допису Instagram про матеріал для /stat СЕМАНТИЧНО: у стрічці інсти немає URL статті (діляться візуалом), тому зіставляємо сигнатуру статті (заголовок+лід із <meta description>) з підписами дописів у вікні дат; лексична схожість (coverage значущих слів) → впевнений збіг без AI, сіра зона → Claude-суддя (Haiku). Метрики допису — views/reach/поширення/збереження через get_media_insights
  stat_tiktok.py          — пошук відео TikTok про матеріал для /stat: TikTok у нас дзеркало інсти (той самий контент і підпис), URL у відео теж немає — тому той самий семантичний матчинг, що stat_instagram (переиспользує його скоринг/сигнатуру/суддю), лише джерело кандидатів — TikTok Display API (get_videos_in_window: video_description+title). Метрики — перегляди/лайки/коментарі/поширення; охоплення TikTok API не дає. Без OAuth (tiktok_analytics) блок TikTok у /stat ховається
  stat_youtube.py         — пошук відео YouTube про матеріал для /stat: те саме семантичне ядро stat_instagram, кандидати з YouTube Data API (youtube_analytics.get_videos_in_window: плейлист завантажень → videos.list, title+description). Метрики — перегляди/лайки/коментарі (поширень/охоплення Data API по відео не дає). Без OAuth (youtube_analytics) блок YouTube ховається
  telegram_stats.py       — перегляди постів каналу @nikvesti (індекс + парсинг t.me/s); channel_subscribers() — підписники каналу (спільний парсер для таблиці аналітики і дашборда Mini App)
  english_report.py       — місячний звіт EN-версії (GA4 + Search Console + AI коментар)
  instagram.py            — тижнева статистика Instagram
  facebook.py             — тижнева статистика Facebook
  social_store.py         — пам'ять соцмереж у Postgres, ДВА грейни: social_stats (тижневі зрізи — FB/IG піггібеком на недільні звіти, Telegram/TikTok/YouTube/Viber окремим тихим захватом у неділю 18:30) і social_monthly (МІСЯЧНА історія всіх шести мереж: пише кожен місячний знімок таблиці аналітики, source='api'; історію таблиці з 2024-02 заливає /social_import_sheet, source='sheet'). /social_capture знімає всі мережі зараз, історія для NLQ-tool get_social_history
  social_import.py        — імпорт місячної історії соцмереж із Google-таблиці «Аналітика МикВісті» в нору (/social_import_sheet [рік|all]): читає ті самі клітинки, у які пише social_sheet (розкладка блоків із його ж констант — імпорт не може розійтися з експортом), ідемпотентно; закриває розрив назад, бо до 27.07 бот писав числа мереж лише в таблицю
  social_sheet.py         — місячний знімок аналітики в Google-таблицю «Аналітика МикВісті» (SOCIAL_SPREADSHEET_ID): лист на рік, місяці рядками, блоки Сайт (GA4 + SC Search/News/Discover) / Facebook / Instagram / Telegram (парсинг t.me, дзеркала TG_WEB_BASES з фолбеком telegram.me) / YouTube (Analytics API через OAuth — перегляди й години перегляду, /youtube_backfill за всю історію; «Контент» — к-сть відео місяця з Data API; «CTR» — вручну, покази Google не віддає жодним API, лише в Studio) / TikTok (Display API) / Viber (підписники — парсинг публічної сторінки запрошення каналу, numberOfWatchers із серверного стейту, env VIBER_INVITE_URL опційно — Channels Post API метрик аудиторії не віддає взагалі; «Постів у канал» — власний лічильник дзеркала Telegram→Viber; «Активні користувачі» лишаються ручними, як «Охоплення» TikTok — API не віддає); старі листи добудовуються самі; дельти MoM/підсумки року (останнє відоме для підписників, LOOKUP(9^99))/YoY/спарклайни — живі формули, стрілки ▲▼ — custom number format, вбудовані графіки; авто 1-го числа 10:30, /sheet_snapshot, /sheet_backfill, /sheet_format, /sheet_migrate_legacy
  youtube_analytics.py    — YouTube Analytics API (+ Data API) через OAuth: refresh-токен власника каналу → перегляди/години перегляду по місяцях (історичний бекфіл), поточні підписники; налаштування OAuth у docstring
  tiktok_analytics.py     — TikTok Display API через OAuth: підписники + вовлеченість по відео місяця (перегляди/лайки/поширення/коментарі); охоплення й історію API не дає (тільки вперед). TikTok ротує refresh token — актуальний у storage.tiktok_oauth; налаштування у docstring
  gmail.py                — перевірка Gmail
  sheets.py               — запис у Google Sheets (Prozorro)
  reactions.py            — обробка реакцій на повідомлення про тендери
  viber_mirror.py         — дзеркало Telegram→Viber: кожен пост каналу @nikvesti (крім репостів/службових) автопоститься у Viber-канал через Channels Post API (звільнити журналістів від крос-постингу). Текст+лінк, фото (picture, найбільший розмір ≤1 МБ), відео (video ≤20 МБ через getFile Telegram, ≤180 с; крупніше — текстом); підпис до відео окремим повідомленням (Viber video підпису не має); будь-який збій медіа → фолбек на текст; альбом (media_group_id) буферизується і публікується ОДНИМ постом — перше фото з підписом+лінком (Viber альбомів не має, кожне медіа окремим постом = спам; решта фото й відео відкидаються, повний набір за лінком; альбом лише з відео → текст+лінк). /viber_setup, /viber_test; env VIBER_AUTH_TOKEN (+ VIBER_WEBHOOK_URL)
  energy_outage.py        — графік відключень електроенергії (off.energy.mk.ua): /outage, /outage_probe, /outage_export (CSV вулиць із чергами)
  notifier.py             — сповіщення про збої scheduled-задач у приват адміну (слухач EVENT_JOB_ERROR + прямий notify_error у модулях з власним try/except)
  ai_usage.py             — звіт про вартість AI-шару: /aicost за місяць + авто-звіт Олегу 1-го числа; оцінка за прайсом моделей із токенів у storage (record_ai_usage)
  usage_report.py         — щоденний звіт адміну про використання бота співробітниками (щодня 09:25 у приват Олегу, за вчора, БЕЗ активності самого Олега): хто які команди викликав, NLQ-питання з використаними tools, складені беки з темами. Збір: команди — TypeHandler track_usage у bot.py, NLQ+tools — query_router (finally), беки — кнопка в news_archive (тема = query пошуку з news_search) і текстові запити беку в NLQ (тема = питання). Стан — storage.bot_usage (день за Києвом, 30 днів). /usage [вчора|YYYY-MM-DD] — ручний зріз (дефолт сьогодні, показує і адміна)
  team_contacts.py        — телефонна база редакції (team_contacts у Норі): два ручні входи — форма в апці й ПЕРЕСЛАНА Лису в приват картка контакту (чат бот не сканує); пошук одним полем по імені/посаді/темах/номеру («хто в нас по енергетиці» — те саме питання, що «дай Лукова»); дедуплікація за останніми 9 цифрами номера; редагують усі, видаляє менеджер; подяка за внесок на дверях бази — БЕЗ впливу на KPI
  team_roster.py          — ростер команди для Mini App «Команда»: відділи Newsroom/Creative/Діджитал-та-дистрибуція (KPI-групи; «Адміністративний» — Олег і Катя), дефолт у коді + фактичний відділ у Норі (team_dept, Катя переносить в апці), прапорець менеджера, резолв initData → людина, кеш tg_id для пінгів
  team_tasks.py           — creative tasks і шари проєктів у Норі (team_creative_tasks/team_project_themes/team_project_order/team_project_deadlines/team_project_drive/team_dept/team_task_events): завдання від редактора (людина × тип news/article/post(+платформа Telegram/Instagram)/будь-який × проєкт CMS або позапроєктне × тематика × к-сть × дедлайн × нотатка; снапшоти partner_name/project_name), статуси open→done/dropped, пінг виконавиці best-effort; тематика має формат (news/article/post/video/hybrid або без); ручний порядок проєктів, дедлайни звітності (наративні/фінансові проміжні-фінальні + майлстоуни), папка Google Drive проєкту
  team_kpi.py             — рекурентні KPI команди (team_kpi_norms/team_kpi_overrides/team_user_link/team_absences у Норі): норма на відділ (метрика news/article × усі/лише власні (own_material) × період тиждень/місяць × ціль на людину; тижнева й місячна — незалежні), правки по людині на період (інша ціль / 0 = звільнена з нотаткою), ПОСТІЙНА ставка людини у відсотках (team_rates — «пів ставки» множить норму назавжди, на відміну від правки, що живе один період; правка сильніша за ставку і тому ЗАВЖДИ підписана в рядку — навіть коли її число дорівнює відділовому, інакше вона тихо глушила б щойно виставлену ставку; правка, що збігається з обчисленою ціллю і без нотатки, не зберігається взагалі), факт сам із nodes по owner_id (резолв: пін team_user_link → ПІБ; кеш 5 хв), періоди за Києвом з зсувом offset → історія будь-якого минулого періоду без снапшотів; відпустка/лікарняна/відрядження (team_absences) пропорційно знижує цілі в усіх зачеплених періодах — рахунок по робочих днях, ручна правка сильніша; робота поза нормою підписана без другої смуги: Creative — «+22 новини в стрічку» (feed_news_counts), Newsroom — «4 власні матеріали 🔥» (own_news_counts), обидва зникають, якщо у відділу зʼявиться норма на те саме; стаття важить ×3 у нормі на новини (ARTICLE_WEIGHT/weighted_facts — окремої норми на статті немає, а забивати на стрічку заради статті редакція дозволяє, тож кружечок за такий місяць не має бути червоним; у рядку KPI видно «+2 статті ×3»); дашборд виконання (kpi_dashboard) і помісячна динаміка людини (kpi_person_history); діагностика /kpi_debug, пін /kpi_link (лікує дублі акаунтів у users)
  team_notifications.py   — стрічка сповіщень апки (team_notifications + team_notification_reads у Норі): подія за роллю (audience managers/person), dedup_key UNIQUE «не смикати одним і тим самим двічі», прочитане ОКРЕМОЮ таблицею по людині (менеджерську подію бачать двоє — read_at у рядку означав би, що Катя прочитала за Олега). Типи: поставили завдання · завдання виконано (з лінком на зараховану публікацію і підписом, ХТО зарахував — «Лис зарахував сам» / «підтвердив(ла) Катерина» / обидва, з status+decided_by матчу) · звітний дедлайн (дубль нагадування з чату фінансів) · імпакт за твоєї участі (медалька з impact_archive). У журналістки стрічка розкладена на два названі блоки — «Нещодавно зараховані» (події, може бути й «1 із 2» при відкритому таску) і «Нові завдання»; рядок події СТРУКТУРОВАНИЙ через meta JSONB (тематика жирним догори, рядок завдання, унизу автор · донор з міні-лого · час — апка малює за макетом, а не текстом; старі події без meta малюються по-старому). Емітери тихі: notify_safe із SAVEPOINT, бо збійний statement у Postgres валить усю транзакцію масової постановки
  team_tg_match.py        — Telegram-пости за дисклеймером (метод Олега): у partner_project є disclaimer_ua, і якщо текст поста каналу (t.me/s, як у telegram_stats) містить його — пост точно цього проєкту, без жодного AI. Автора Telegram не показує → матч ЗАВЖДИ pending: Катя обирає і кому, і в яку тематику. Записуємо лише збіги (інакше осідали б тисячі звичайних постів), уже вирішені відсікає known_refs. /match_tg [сторінок]
  team_matches.py         — пам'ять зарахованого виконання (team_task_matches у Норі): UNIQUE(source, ref) — одна публікація судиться раз, скільки б разів не ганяли прогін; статуси auto/pending/confirmed/rejected/skipped (у прогрес ідуть auto+confirmed); прогрес «2/3» і лінки зарахованих публікацій у картках; для спірних — опис і зображення (og:description/og:image) на картку черги; авто-закриття таска при наборі qty і повернення у відкриті, коли підняли qty чи відкликали матч (закритий руками не чіпається — колонка auto_done)
  team_matching.py        — авто-облік виконання creative tasks: БД сайту каже проєкт публікації, але не тематику → пре-фільтр без AI (відкриті таски автора в проєкті, сумісні за типом, з незакритим залишком) + AI-суддя (Sonnet, tool use → matched_task_id/confidence/reasoning): high → авто-зарахування, не-high → черга Каті у «Сповіщеннях». Фоновий прогін щогодини :45 — ОДИН запит у БД сайту по свіжих проєктних нодах (ліміти сайту), AI лише там, де кандидати є; збій судді нічого не пише, наступна година повторить. суддя не пише обґрунтування (зайві токени — у картці й так заголовок, опис і зображення), а для тематик про критичні інформаційні потреби в промпт додається рубрика CIN (8 напрямів, методологія Annenberg/MDF) — лише коли така тематика серед кандидатів. /match_scan [днів] — прогнати зараз, /match_test <url> — прогін на статті, НІЧОГО не зараховує
  team_analytics.py       — аналітичний дашборд Mini App (таб «Аналітика» на Головній): читає ВЖЕ накопичене — сайт по днях (daily_stats), розріз пошуку web/discover/googleNews (sc_daily_stats), тижневі зрізи соцмереж (social_stats) + живі підписники Telegram, вихід редакції одним запитом у nodes (новини/статті/власні/автори), гранти (проєкти CMS + team_creative_tasks/team_task_matches), топ матеріалів із денних топ-5 (daily_stats.top_pages). Тиждень/місяць, дефолт — попередній; НЕЗАВЕРШЕНИЙ період порівнюється з тією самою кількістю днів попереднього (інакше у вівторок завжди −70%); дані не далі ніж вчора (захват о 09:00). Нічого не пише, кеш 180 с, блоки деградують окремо
  team_projects.py        — проєкти/партнери/фото з БД сайту для Mini App: partner_project (квоти kpi_news/kpi_articles, строки) + partner (лого), users.avatar → URL через ресайзер (/255x255/img/users_ava/... і /images/partner_logo/..., завжди .webp), матч фото до ростера за ПІБ, кеш 10 хв (ліміти БД сайту)
  report_reminders.py     — нагадування про звітні дедлайни грантів у чат «Фінанси МикВісті» (FINANCE_CHAT_ID): за тиждень і за 2 доби, ідемпотентно через team_deadline_reminders; /reports — ручний прогін
  impact_archive.py       — імпакт-архів (impacts/impact_articles/impact_credits у Норі): журнал впливу для донорських заявок. Кинув URL новини-фіксації (+суть своїми словами) — бот сам збирає серію (беклінки з живої сторінки + FTS нори), авторів і проєкт/донора кожного тексту з БД сайту, а Sonnet (tool use impact_case) складає донорський заголовок, наратив («що сталось» + «Значення та вплив»), КЛЮЧОВИЙ текст серії і медальки (кому записати, з нотаткою). Усе — чернетка: ключовий перепризначається, серія ріжеться, імена правляться. Донор ключового тексту підсвічений («можна порадувати донора»). Експорт — .html файлом у приват (друк у PDF з браузера). Збір асинхронний, збій → failed + «спробувати ще». Деталі — docs/IMPACT_ARCHIVE.md
  team_todos.py           — блокнот (особистий список справ, team_todos у Норі): два кошики «Сьогодні»/«Потім», мʼякий поріг 6 справ на день, вік пункту, приватний повністю (person із initData у кожному WHERE, менеджерського перегляду немає й не буде). Прототип зібрано з дослідження: 41% пунктів не роблять ніколи, а половину зроблених закривають у день запису → все вирішує швидкість захоплення (Enter лишає фокус + /todo у чаті); Masicampo & Baumeister — вантаж знімає ПЛАН, а не виконання → два кошики; Gollwitzer (d=0.65) — намір «коли» подвоює виконання → «сьогодні чи потім» як найдешевша його форма; Айві Лі — сила в ліміті → поріг вголос; Amabile — видимий прогрес → «Зроблено сьогодні · N». Пріоритетів, дедлайнів, тегів і підзадач немає свідомо
  team_publications.py    — «Мої публікації» для Mini App: перелік того, що вийшло під іменем людини за місяць (nodes БД сайту — те саме джерело, що рахує факт KPI, тож перелік і кружечок норми розійтися не можуть), із власним лічильником переглядів сайту. НЕ /stat: походи у FB/IG/TikTok/YouTube/GA4 по кожному рядку для щоденного екрана неприйнятні, тож соцмереж тут немає. Лічильник переглядів накопичувальний ЗА ВЕСЬ ЧАС — так і підписаний («на сайті»), без «за місяць»
  webapp.py               — aiohttp-сервер Mini App поруч із polling (post_init, стартує лише з PORT): перевірка HMAC initData, API (/api/bootstrap одним запитом: люди+фото, проєкти+тематики+дедлайни+Drive, таски; /api/dashboard аналітичний дашборд за період; /api/kpi зведення за роллю, /api/kpi/dashboard за період, /api/kpi/person помісячна динаміка; /api/publications «Мої публікації» за місяць; /api/todos блокнот (GET/POST/PATCH/DELETE, лише свій); /api/impacts імпакт-архів (CRUD + retry + send файлом у приват, менеджерське); менеджерські POST/PATCH tasks (+tasks/bulk — масова постановка), themes, projects/order, projects/{id}/drive, project_deadlines, people/dept, kpi/norms, kpi/override), обидві БД деградують окремо, статика webapp/ (на старті гзіпиться в .gz-сусідів — aiohttp віддає їх сам, ~101 КБ → ~26 КБ — і отримує версію-хеш у ?v=, тож старий JS проти нового API неможливий), /health (придатний як VIBER_WEBHOOK_URL), команда /team
  helpers.py              — спільні утиліти (парсинг місяців, get_author_from_url, extract_article_id, normalize_https_url — добудова https:// до env-URL без схеми)
  query_router.py         — Intent Router: природномовні запити до Лиса (GA4 + Search Console, tool use)
webapp/                   — SPA Mini App «Команда» без збірки (index.html + style.css + app.js): біло-синьо-голуба гама, SVG-спрайт Feather (без емодзі-кнопок), повітря, теми Telegram. Редакторський флоу: ГОЛОВНА з трьома табами Аналітика/Завдання/Звіт (відкривається остання обрана, localStorage) — «Аналітика» це дашборд редакції (геройське число читачів + стовпчики днів, плитки з дельтами, частки пошуку складеною смугою з легендою, вихід редакції з барами авторів → тап веде в трекер, соцмережі з приростом підписників, гранти, топ матеріалів); «Завдання» показує лише людей із відкритими тасками (кружечок, розбивка по донорах «IMS ×3 · IWPR ×2», лічильник), тап → персональний трекер (Загальні задачі = KPI зі шторкою правки + Проєктні задачі по донорах); «Звіт» — дашборд виконання KPI за період (тиждень/місяць, гортання ‹період› назад; перемикач переїжджає одразу, а замість старих цифр стає скелетон — факт іде в БД сайту і думає секунди) кружечками з кільцем % (колір червоний→жовтий→зелений), тап → профіль зі стовпчиковою динамікою місяць-до-місяця. Bottom-menu: «＋» → людина → форма creative task; Проєкти (список «донор великим + строки» з drag-n-drop порядком (довгий натиск, картка їде за пальцем, сусіди роз'їжджаються) / таймлайн-ґант із липкими лейблами і точкою «сьогодні»; всередині проєкту — тематики з форматами і розкроєм квот (тап по тематиці → МАСОВА постановка: місяць + люди зі степерами), дедлайни звітності, папка Drive); KPI — лише налаштування норм; Команда — тап трекер, олівець перенесення між відділами. Картинки: ресайз .webp → фолбек оригінал → ініціали. Журналістка — «Мої KPI» (тиждень+місяць, із поясненням ваги статей), далі відкриті таски (тематика першим рядком, донор дрібним), під ними блок «Виконані», далі стрічка подій двома блоками («Нещодавно зараховані» / «Нові завдання»), внизу згорнутий блок «KPI по місяцях» (localStorage myHistOpen). У журналістки СВОЄ нижнє меню (Головна · Публікації · Блокнот · Контакти · Події): регулярне живе там, на головній лишились двері, які без числа не мають сенсу («Виконані ×3», «66%», «Не буду на роботі»). «Публікації» — вихід за місяць із гортанням назад, геройське число переглядів сайту. Перегляд чужими очима (менеджер) показує ТОЙ САМИЙ екран і ходить його підекранами, лише «Події» і «Не буду на роботі» вимкнені (діяли б від імені людини)
  news_archive.py         — архів новин сайту (БД): пошук "що ми писали про X", ліди, генерація беку, кнопка "Написати бек"
  news_stats.py           — підрахунки по БД сайту (nodes): NLQ-tool count_news — скільки матеріалів за період / власних (own) / по рубриках / по авторах (owner_id→users) / мовою (ua/ru/en, EN-колонку визначає інтроспекцією); metric=views сумує перегляди (лічильник сайту, за весь час) — «хто з журналістів набрав більше переглядів». Джерело істини, свіже, рахує й англ. версію (якої немає в норі). ПЛЮС NLQ-tool list_news — перелік опублікованого за період (заголовок, час виходу, автор за owner_id, лінк, ознака own) — «що вийшло сьогодні і хто автор», «скільки своїх сьогодні і які»; НЕ плутати з GA4-топом за переглядами
  tags_wikidata.py        — прив'язка тегів сайту до Q-сутностей Wikidata для schema.org about: /tags_export (топ-N тегів у CSV), /tags_wiki (топ-N → Wikidata wbsearchentities → Claude добирає QID+@type → CSV на ревʼю + готовий ALTER/UPDATE .sql, бо БД сайту read-only)
  knowledge_graph.py      — розвідка Google Knowledge Graph Search API: /kg <KG ID або запит> дістає канонічну картку сутності (name, @type, опис, Вікіпедія, сайт, resultScore), перевірка як Google бачить бренд/тег; простий API key GOOGLE_KG_API_KEY
  budget_revisions.py     — бюджет Миколаєва в норі (схема budget, «задание №2»): кинув ZIP пакета рішення в приват — бот сам розбирає (тип таблиці за шапкою, бо імена файлів брешуть; дублі по md5), права частина «Порівняльної таблиці» → нова ревізія, ліва → валідація (міжсесійні зміни + одруківки депфіну → revision_validation_issue, пари XX00000/XX10000), headline з PDF рішення, нульовий PDF-пакет → порожній original з добудовою; дельти v_plan_amendments
  budget_execution_report.py — квартальні звіти про виконання (офіційний пакет «Звіт про виконання … за N квартал», казначейські форми): виконання ПО КОЖНІЙ ПРОГРАМІ (КПКВК, Додаток 13 розподілу видатків + доходи), стикується з ревізіями по КПКВК; кинув ZIP у приват — бот сам розпізнає й покаже розпорядників і найбільші програми з найгіршим % виконання; /budget_execution бере квартальний пріоритетно (детальніше за місячний снапшот)
  budget_snapshots.py     — місячні снапшоти виконання бюджету («задание №1»): монітор «Щомісячної інформації» mkrada (щодня 11:20, перший запуск бекфілить рік тихо), розріз по розпорядниках КВК×КЕКВ/КБП (Лист1/Лист2, чого не дає OpenBudget API — там лише ТПКВК консолідовано) + доходи за назвами; /budget_execution — хто з розпорядників не виконав план періоду (<80%) + звірка з ревізіями рішень (різниця = міжсесійні зміни ± методологія знімка); снапшот-xlsx можна кинути в приват і руками
```

---

## Команди бота

| Команда | Що робить |
|---|---|
| /start | Привітання зі списком команд |
| /status | Перевірка що бот живий |
| /analytics | GA4 статистика за вчора + топ-5 статей |
| /analytics_backfill \[N\] | Залити N днів історії трафіку з GA4 у daily_stats (дефолт 90; можна роками — GA4 тримає стандартні звіти весь час, ллється чанками по року); далі наповнюється сам тихим захватом о 09:00 |
| /sc_backfill \[N\] | Залити N днів денного розрізу Search Console по типах (web/discover/googleNews) у sc_daily_stats (дефолт 90; SC тримає ~16 міс); далі наповнюється сам тихим захватом о 09:00 разом із GA4 |
| /weekly | Тижневик Лиса вручну в чат редакції |
| /social_capture | Зняти зріз УСІХ мереж зараз у social_stats (FB, IG, Telegram, TikTok, YouTube, Viber) — засів першої точки; далі знімок сам щонеділі |
| /social_backfill_fb \[міс\] | Спроба залити історію FB (перегляди/взаємодії по тижнях) за N місяців (дефолт 24) через since/until; вставляє лише відсутні тижні. IG історію Meta не віддає |
| /social_import_sheet \[рік\|all\] | Залити місячну історію соцмереж із Google-таблиці «Аналітика МикВісті» в нору (усі мережі, з 2024-02): те, що бот раніше писав тільки в таблицю. Ідемпотентно, дефолт — усі річні листи |
| /sheet_snapshot \[YYYY-MM\] | Знімок місяця в Google-таблицю «Аналітика МикВісті» вручну (дефолт — попередній місяць); ідемпотентно, рядок місяця перезаписується. Авто — 1-го числа о 10:30 |
| /sheet_backfill \[міс\] | Залити історію в таблицю аналітики (дефолт 36 міс, від давніх до свіжих): сайт — уся глибина GA4, SC ~16 міс, FB/IG — скільки віддасть Meta (~2 роки), TG — стрічка t.me; підписники заднім числом недоступні ніде |
| /sheet_format \[рік\|all\] \[dark\|light\] | Примусово перекатити оформлення річного листа таблиці аналітики (шапки з лого, формати ▲▼, формули, спарклайни, графіки, тема); all — усі річні листи разом; дані місяців не чіпає. Дефолтна тема — dark (env SOCIAL_SHEET_THEME); тема документа глобальна для всіх листів. «Голий» лист бот лікує й сам при наступному записі |
| /sheet_migrate_legacy | Разово перенести історію зі старої ручної таблиці «МикВісті SMM» (2024-02…2026-06, data/legacy_smm.json) у таблицю аналітики: підписники всіх мереж + IG/FB/TG метрики + YouTube/TikTok/Viber цілком; ідемпотентно |
| /youtube_backfill \[рік\] | Залити помісячну історію YouTube (перегляди відео → D, години перегляду → F) з YouTube Analytics API одним запитом за всю історію каналу (дефолт старту 2022); потрібен OAuth (YOUTUBE_OAUTH_*). Ідемпотентно |
| /tiktok_auth \[code\] | Разова авторизація TikTok (Sandbox): без аргументів — бот дає посилання згоди; /tiktok_auth \<code\> — обмінює код на refresh token і зберігає у storage.tiktok_oauth. Потрібні TIKTOK_CLIENT_KEY/SECRET (Sandbox) + TIKTOK_REDIRECT_URI |
| /reports | Прогнати нагадування про звітні дедлайни зараз: показує, що полетіло б у «Фінанси МикВісті» (у чат, де викликано; планових позначок не з'їдає) |
| /todo \[текст\] | Записати справу в особистий блокнот прямо з чату (думка приходить у чаті, а не в апці). Кладе в «Потім» + кнопка «Зробити сьогодні» одним тапом; без тексту — короткий підсумок і кнопка апки |
| /team | Кнопка відкриття Mini App «Команда» (таски + проєкти + KPI): у приваті — нативна web_app (у групі Telegram web_app-кнопки не приймає), у групі — лінк t.me, який бот збирає сам із власного username (WEBAPP_DIRECT_LINK лише як override); без налаштованого WEBAPP_URL пояснює, чого бракує |
| /kpi_debug \<прізвище\> | Чому у людини такий факт KPI: до якого users.id привʼязано (пін чи ПІБ), усі записи users із цим прізвищем (ловить дублі акаунтів), розкладка виходу за місяць owner_id × type × own_material, кроки тижнів з накопиченням і список відсутностей (відповідає на «чому цей кружечок в апці порожній»); підказує /kpi_link, коли весь вихід під іншим id |
| /kpi_link \<прізвище\> \<users.id\> | Закріпити правильний users.id за людиною (перебиває пошук за ПІБ) — лікує дублі акаунтів у users без деплою |
| /match_estimate \[YYYY-MM\|all\] | Оцінка ретроспективного прогону звірки (read-only, безкоштовно): скільки публікацій підпаде під вікна тасків і скільки коштуватиме суддя. Без аргументу — поточний місяць |
| /match_backfill yes \[YYYY-MM\|all\] | ПЛАТНО: ретроспективний прогін — догнати вже виконані таски заднім числом. Вікно від кожного таска (min(створення, перше число місяця дедлайну)), межа глибини — поточний місяць за замовчуванням. **Пінги приглушені**, зведення в кінці. Ідемпотентно — ганяти можна скільки завгодно |
| /match_requeue \<url\|id\> | Повернути публікацію в чергу «Сповіщень»: рятунок, коли рішення виявилось помилковим уже після ухвалення (зарахували не тому, зняли, таск видалили — і матеріал завис у rejected, тобто ні в черзі, ні у судді) |
| /match_cards \[N\] | Доодягнути картки черги зараз: опис і зображення (og:description/og:image) зі сторінок матеріалів. Те саме робить щогодинний прогін по 50 за раз — команда потрібна після великої ретроспективи, щоб не чекати годину |
| /match_tg \[сторінок\] | Переглянути пости каналу за дисклеймерами проєктів зараз (авто — разом зі щогодинним прогоном :45): проєктні пости йдуть у чергу «Сповіщень», бо автора Telegram не показує. AI не задіяний |
| /match_scan \[днів\] | Прогнати звірку виконання зараз (дефолт 7 днів; авто — щогодини :45): свіжі проєктні публікації → суддя → high зараховується в таски, решта йде в чергу «Сповіщень» апки. Ідемпотентно — повторний виклик нічого не подвоїть |
| /match_test \<url\> | Прогін судді-матчера на реальній статті: кандидати з відкритих тасків автора в проєкті публікації + вердикт (тематика, впевненість, обґрунтування). НІЧОГО не зараховує — діагностика перед авто-обліком |
| /viber_setup | Налаштувати дзеркало Viber: зареєструвати webhook (VIBER_WEBHOOK_URL) і показати акаунт/супер-адміна каналу. Потрібен VIBER_AUTH_TOKEN |
| /viber_test | Тестовий пост у Viber-канал (перевірка постингу) |
| /report | GA4 звіт за вчора в чат редакції (щоденний авто-пост прибрано, лишилась ручна команда) |
| /checkmail | Перевірити Gmail |
| /instagram | Тижнева статистика Instagram |
| /igreport | Тижневий Instagram звіт з AI в чат |
| /facebook | Тижнева статистика Facebook |
| /fbreport | Тижневий Facebook звіт з AI в чат |
| /morning | Ранкове повідомлення вручну |
| /documents | Перевірити нові документи органів влади |
| /documents_test | Тестовий пост з першого документа кожного джерела |
| /documents_rebaseline | Зберегти поточні сторінки як бачені БЕЗ відправки (гасіння спаму) |
| /competitors | Перевірити новини конкурентів |
| /law | Перевірити новини правоохоронних органів |
| /stat \<url\> | Статистика матеріалу (Facebook + Instagram + TikTok + YouTube + Telegram + GA4) за URL nikvesti.com. Instagram/TikTok/YouTube шукаються семантично (по підпису, бо URL у стрічці немає); блоки TikTok і YouTube — лише коли налаштовано їхній OAuth |
| /stat_forget \<url\|id\> | Забути матеріал у Норі (article_stats) та індексі TG (tg_posts) — наступний /stat пройде повний живий пошук по всіх каналах (демо, діагностика матчингу) |
| /english | Місячний звіт EN-версії сайту (GA4 + Search Console) |
| /prozorro | Перевірити тендери Prozorro |
| /prozorro_test_jump \[N\] | Діагностика зсуву офсету за N днів (дефолт 14) |
| /prozorro_confirm_jump \[N\] | Підтвердити скидання офсету |
| /prozorro_reset_tender \<id\> | Розблокувати тендер для повторної реакції |
| /traffic | Хто зараз на сайті (GA4 Realtime) + типовий трафік для цієї години |
| /reset | Забути контекст розмови з Лисом (пам'ять діалогу NLQ) |
| /aicost \[YYYY-MM\] | Витрати AI-шару за місяць (дефолт — поточний) |
| /usage \[вчора\|YYYY-MM-DD\] | Зріз користування ботом за день (дефолт — сьогодні): команди, NLQ-питання, tools, беки по людях; показує і адміна. Авто-звіт за вчора без адміна — щодня 09:25 Олегу в приват |
| /stat_backfill \[N\] | Разовий бэкфіл індексу постів каналу для /stat (N місяців, дефолт уся історія) |
| /outage | Графік відключень електроенергії (off.energy.mk.ua) |
| /outage_probe \<path\> \[arg\] | Службова розвідка API з Railway (тимчасово) |
| /outage_export \[idfilial\] | CSV вулиць із чергами, дефолт 15 (Миколаїв), ~7–15 хв |
| /outage_geocode \[idfilial\] | Геокодує вулиці по мікрорайонах (дефолт 15, Миколаїв) |
| /myip | Вихідний IP Railway + чи він у whitelist БД сайту (діагностика конекту) |
| /dbtest | Перевірка з'єднання з MySQL-БД сайту (версія, база, к-сть таблиць, час відповіді) |
| /dbquery \<SELECT…\> | Службова розвідка схеми БД з Railway (read-only, тимчасово) |
| /builder | Діагностика монітора білдера: коли оновлювався, скільки власних новин відтоді |
| /builder_test | Надіслати зразок нагадування про білдер у чат редакції (в обхід порогів) |
| /fbmissing | Прогнати монітор миколаївських новин без ФБ зараз (ігнорує гейт годин, поважає «раз»); алерти — в чат, де викликано |
| /fbmissing_test | Прев'ю формату підказки+чернетки на найсвіжішій миколаївській новині, БЕЗ зміни стану (можна ганяти скільки завгодно) |
| /dossier \<тема\> | Історія питання з 17-річного архіву: таймлайн по роках з лінками на матеріали |
| /tags_export \[N\] | Топ-N тегів сайту за ужитком у CSV (дефолт 300), з розмерджуванням redirect_tag_id |
| /tags_wiki \[N\] | Зіставити топ-N тегів (дефолт 100) із Wikidata: QID + @type + посилання на Вікіпедію (uk→ru→en) + Google KG ID (P2671, безкоштовно з Wikidata). CSV на ревʼю + готовий ALTER/UPDATE .sql (4 колонки) для PHPMyAdmin (БД сайту read-only). Кешується за tag_id — повторний прогін на більший N чіпає лише нові теги, файли віддає на весь N |
| /tags_wiki_reset | Скинути кеш зіставлення тегів (наступний /tags_wiki перерахує все з нуля) |
| /kg \<KG ID або запит\> | Картка сутності з Google Knowledge Graph Search API: name, @type, опис, Вікіпедія, сайт, score. По ID (/g/…, /m/…) або за назвою. Потребує GOOGLE_KG_API_KEY |
| /archive_sample | Залити кілька старих+нових статей і показати збережений текст (перевірка чистки/розділення мов) |
| /archive_backfill \[N\] | Заливка архіву в дзеркало; N — порція за запуск (фазування), без N — усе; resumable |
| /articles_backfill \[N\] | Разовий долив історичних статей (type='article') у нору — головний бекфіл їх проминув, коли фільтр був type='news'. Свій курсор у sync_state (основних не чіпає), resumable, зупинка — /archive_stop; справа хвилин. Свіжі статті далі веде щогодинний інкремент |
| /archive_stop | М'яко зупинити поточний бекфіл (після поточної пачки); resumable — повторний /archive_backfill продовжить з місця зупинки |
| /archive_status | Стан дзеркала архіву: скільки статей, діапазон дат, курсори синку |
| /archive_report | Здоровкове зведення нори для нагляду за бекфілом: розподіл по роках, мови, теги, рубрики, регіони, середня довжина тексту по роках (детектор проблем чистки) |
| /nora_sql \<SELECT…\> | Read-only запит до нори (Postgres бота) для ad-hoc нагляду; той самий підхід, що /dbquery для БД сайту |
| /nora_article \<id\|URL\|заголовок\> | Витягти матеріал із нори текстовим файлом — рятунок, коли на сайті його затерли/видалили/зіпсували. Шапка (id, вихід, час знімка в норі, автор, рубрика, теги, URL) + текст усіх мовних версій. Кілька збігів за заголовком → список id |
| /nora_resync \<id\|URL\> … | Примусово перечитати конкретні ноди з БД сайту в нору, В ОБХІД курсора. Потрібно, коли CMS підмінила вміст ноди, не зачепивши `updated` — такої зміни щогодинний інкремент не бачить ніколи (реальний кейс 28.07: матеріал відновили під новим id, у старому лишився чужий текст зі старим updated) |
| /entity_estimate \[з\] \[по\] \[тип\] | Оцінка бэкфіла сутнісного шару: к-сть статей до витягу + скільки вже розібрано (за них не платимо) + вартість Batch API (read-only, безкоштовно; дефолт 2022-01-01..2027-01-01, тип all). Тип: articles \| news \| all |
| /entity_backfill \<з\> \<по\> \[тип\] | ПЛАТНО: витяг сутностей із матеріалів діапазону через Batch API (Haiku 4.5, −50%), полінг у фоні, по готовності — злиття в entities/article_entities і звіт у чат. **Тип** (articles \| news \| all, дефолт all) критичний для ціни: статей на порядок менше за новини, тож догнати спершу їх коштує пару доларів замість сотень за роки новин. Уже розібране пропускається — повторний прогін не платить удруге і не нашаровує зв'язки поверх старих (свідомий перечит — /entity_resync) |
| /entity_status | Сутнісний шар: скільки сутностей по kind, зв'язків, топ за згадками + стан активного прогону батчів + черга авто-інкремента (скільки статей чекає добору, орієнтовна вартість, останні причини збоїв витягу) |
| /entity_resume | Переприв'язати полінг батчів після редеплою (стан у sync_state) або повторити збій ingest (ідемпотентно) |
| /entity_resync \<id\|URL\> … | Перечитати сутності конкретних статей (платно, ~$0.006/стаття). САМ спершу тягне свіжий текст із сайту в нору (щоб правка в CMS не загубилась: витяг читає нору, а не сайт): спершу знімає всі article_entities статті, потім витяг заново — інакше write_results лише апсертить зв'язки і стаття тягла б за собою сутності від затертого тексту. До 20 статей за раз |
| /entity_recover | Коли стан прогону загублено: знайти батчі на боці Anthropic (живуть 29 днів), пересобрати стан і довести до ingest; якщо батчів немає — гроші не витрачались |
| /entity_increment_on | Увімкнути авто-інкремент сутностей: щогодини о :55 статті нори без сутностей проходять витяг Haiku (~$0.006/стаття, звичайний API, в /aicost). Підлога добору — початок уже покритого діапазону: інкремент веде свіже І затягує дірки в ньому; глибше — територія бэкфілу |
| /entity_increment_off | Вимкнути авто-інкремент сутностей |
| /entity_export | CSV усіх сутностей (kind/імена/роль/згадки/аліаси, за спаданням згадок) — для ручного QA поза ботом |
| /entity_dedup | Разове глобальне переслияння дублів (точний збіг norm у межах kind): перевішує зв'язки, зливає аліаси, видаляє зомбі-картки, перераховує агрегати; ідемпотентно |
| /entity_scale | Масштаб дублів у норі числами (read-only, нічого не змінює): ОКРЕМО картки — скільки пар зі схожим написанням (similarity > 0.8), скільки карток-одноденок (≤2 згадки) по kind, скільки випадків «одне рос. написання, різні укр.» («Кім/Ким»); ОКРЕМО ролі — скільки сирих написань і скільки лишається після нормалізації, скільки людей носять >3 різні ролі, найчастіші ролі й скільки пар-кандидатів знайшов би детектор. Від цих чисел залежить, чи потрібні детектор карток, черга і суддя |
| /entity_classes \[поріг\] | Розкладка кандидатів на злиття по КЛАСАХ (read-only), окремо картки й ролі: ті самі слова в іншому порядку · скорочення/розгортання (лише коли решта слів збігається — інакше «стаття 190 КК» і «стаття 310 Кримінального кодексу» злились би) · уточнення (з показом зайвого слова і поділом «доповнює назву» / «розрізняє») · одне слово замінено (з показом обох слів) · друкарська різниця · лексично різні зі спільним носієм. Поріг схожості для карток — дефолт 0.8, той самий, що в /entity_scale (аргументом можна знизити; нижче 0.8 сипле сміттям на кшталт «вулиця Повстанська» ~ «вулиця 4 Ялтинська») |
| /entity_merge_log \[N\] | Журнал злиттів карток: що з чим злилось, коли, скільки зв'язків на кону, чи відкочено |
| /entity_unmerge \<id\> | Відкотити злиття зі знімка: повертає картку з тим самим id, її зв'язки разом із role_at_time і salience, знімає з переможця дописані аліаси та статті, перераховує агрегати обох. Кластер із кількох карток відкочувати у зворотному порядку (спершу більший id) |
| /roles | Стан канону ролей: скільки сирих написань, скільки зведено, покриття зв'язків, скільки канонів з афіліацією, довжина черги питань |
| /roles_dedup \[N\] | Прогнати детектор дублів РОЛЕЙ і питати інлайн-кнопками: «мер Миколаєва» це те саме, що «міський голова»? [Так, одне] [Ні, різні] [Пропустити]. Після «так» другим питанням — чий це орган (дає афіліацію людина↔організація). Сирий role_at_time не переписується; «ні, різні» пам'ятається назавжди |
| /roles_bulk | Підтвердження КЛАСОМ, а не парою: список класів у черзі з к-стю, тап → десяток прикладів → «звести всі N» / «відхилити всі N». Потрібне тому, що кандидатів 2109 і по одній це десяток годин, а на третій сотні людина перестає читати підстави. Гурт ніколи не діє без показу прикладів; класи «різні номери», «інший регіон», «розрізняє» позначені як «зазвичай ні», двоїсті (спільний носій, одне слово замінено) — «тільки по одній» |
| /roles_canon \[N\] | Список канонів посад: усі написання кожного + орган |
| /roles_rename \<id\> \<текст\> | Переназвати канон посади (id підкаже /roles_canon) |
| /roles_outliers \[N\] | Рідкісні носії сталої посади (read-only): у статті про Миколаївщину бек «у Херсоні теж… голова ОВА Прокудін» — і витяг ДОПОВНИВ роль регіоном статті. АЛЕ рідкісний ≠ помилковий: у 17-річному архіві Чайка (мер 2001–2013) при Сєнкевичу (з 2015) абсолютно законний. Тому звіт розкладає знахідки: «схоже ім'я» = дубль КАРТКИ або однофамілець (вісь карток), «інша людина» = попередник або помилка витягу, і поруч друкуються ПЕРІОДИ обох — у попередника свій відрізок часу, у помилки одна згадка посеред періоду головного носія. Список id для /entity_resync дається лише для другої купи; сирий role_at_time не чіпає |
| /roles_org \<id канону\> \[id сутності\|0\] | Переглянути/змінити/зняти орган посади. Питання про орган ставиться раз, одразу після зведення, і легко промахнутись — виправляється без деплою. Кандидати: спершу за НАЗВОЮ посади («міністр юстиції» → «Міністерство юстиції України»), потім за співпоявою; співпояв ловить сюжет, а не афіліацію (Галущенку він пропонував НАБУ й «Квартал 95») |
| /roles_forget \<текст\|id\> | Відкат канону: написанням — зняти його з канону, числом — розібрати канон цілком. Пари повертаються в чергу питань, сирий текст ролі в статтях не чіпається |
| /entity_export_links \[N\] | Граф співпоявності для візуалізації: CSV вузлів (топ-N сутностей, дефолт 150) + CSV ребер (пари зі спільними статтями ≥3, з вагами) — формат nodes/edges для Gephi/d3 |
| *(ZIP у приват, без команди)* | Пакет рішення сесії (нульовий бюджет чи зміни) → бот сам розбирає і нашаровує ревізію бюджету в схему budget; порядок = порядок надсилання. Підпис до файлу «26.03.2026 51/47» задає дату ухвалення й ухвалений номер (у пакеті-проєкті їх немає) |
| /budget_load \[рік\] \[номер\] \[дата\] | Ручне завантаження одного xlsx (підпис до документа або reply); без аргументів усе визначається з шапки файлу |
| /budget_status | Ревізії бюджету по роках: рядки, суми видатків/доходів, розбіжності валідації |
| /budget_headline \<рік\> \<номер\> ключ=значення … | Поправити показники текстової частини рішення вручну (авто тягнуться з PDF рішення в пакеті) |
| /budget_date \<рік\> \<код\> \<DD.MM.YYYY\> \[номер\] | Задати дату ухвалення ревізії (у пакеті-проєкті її немає) і опційно канонічний номер рішення замість коду проєкту (s-fi-003 → 51/47); дату можна також дати підписом до ZIP |
| /budget_execution | Виконання бюджету: пріоритетно офіційний квартальний звіт (по КПКВК-програмах + розпорядниках), інакше останній місячний снапшот; розпорядники й програми з найгіршим % виконання |
| *(ZIP «Звіт про виконання … за N квартал» у приват)* | Квартальний казначейський пакет → бот бере Додаток 13 (видатки по КПКВК) + доходи, рахує % виконання по програмах і розпорядниках |
| /budget_snapshot_check | Перевірити сторінку «Щомісячна інформація» mkrada зараз (авто — щодня об 11:20); снапшот-xlsx можна кинути в приват і руками |
| /budget_execution_test | Надіслати зразок анонсу «Микита винюхав…» + розбір виконання у канал «винюхав» в обхід seen (перевірка формату без нового файлу на сайті) |

---

## Природномовні запити (Intent Router)

Приватне повідомлення боту (від `ALLOWED_USER_IDS`), або reply на повідомлення бота в чаті редакції — йде в `handle_natural_language_query` (`handlers/query_router.py`), Claude сам обирає tool через tool use (GA4, історія трафіку з daily_stats через `get_traffic_history` — тренди й порівняння період-до-періоду дешево з локальної БД, Search Console (свіже напряму — `get_search_console_report`; розріз трафіку по типах web/discover/googleNews з локальної пам'яті sc_daily_stats — `get_search_console_history`, для «трафік без Discover»/«частка Discover» дешево і без обриву відповіді; AI-трафік Google окремо в API не віддає — він у web), архів тендерів Prozorro, Facebook/Instagram поточні + історія соцмереж через `get_social_history` з social_stats, архів новин сайту, підрахунки матеріалів через `count_news` з `handlers/news_stats.py` — скільки вийшло за період / власних / по рубриках / по авторах / мовою ua/ru/en, перелік опублікованого за період через `list_news` («що вийшло сьогодні і хто автор», «скільки своїх сьогодні і які» — заголовок, час, автор, лінк, own з БД сайту), бюджет Миколаєва через `query_budget` з `handlers/budget_nlq.py` — «кому дали +139 млн?» з розпорядником, нові програми, історія рядка по ревізіях, «які зміни були в березні» (фільтр month за датою ухвалення), «хто не виконує бюджет» (пріоритетно квартальний звіт по КПКВК, інакше місячні снапшоти), плюс графіки: `execution_trend` (динаміка % виконання по місяцях) і `budget_growth` (розмір бюджету по ревізіях, млрд) → render_chart) і відповідає живою мовою. Питання "що ми писали про X?" шукає по заголовках новин у БД сайту (`handlers/news_archive.py`) і відповідає списком "дата — заголовок (лінк)" з кнопками відбору: номерні чекбокси ✅ + кнопка "🦊 Бек з усіх цих новин"/"Бек з новин 1+3"; бек ("Нагадаємо, раніше…") складається з лідів вибраних новин, лінки — анкорами (≤3 слова) всередині речень. Стан пошуку/вибору — в storage (`news_search`), переживає редеплой. Лис пам'ятає останні 6 обмінів протягом 30 хв (follow-up'и "а за минулий місяць?" працюють), `/reset` скидає. Деталі — [`docs/NATURAL_LANGUAGE_QUERIES_MODULE.md`](docs/NATURAL_LANGUAGE_QUERIES_MODULE.md).

---

## Розклад (Europe/Kiev)

| Час | Що запускається |
|---|---|
| 08:15 щодня | Ранкове повідомлення в чат редакції |
| 09:00 щодня | Тихий захват вчорашньої аналітики в daily_stats (users/sessions/pageviews + топ сторінок) + денний розріз Search Console по типах у sc_daily_stats (web/discover/googleNews, трейлінг-вікно), БЕЗ поста — щоденний звіт у чат прибрано на користь тижневика |
| 09:25 щодня | Звіт про користування ботом за вчора (команди/NLQ/tools/беки по людях, без активності адміна) — у приват Олегу; тихий день — короткий рядок |
| Понеділок 09:30 | Тижневик Лиса: тиждень сайту до тижня (users/sessions/pageviews) + топ-5 матеріалів + винюхані тендери + соцмережі тиждень-до-тижня (з social_stats) + AI-підводка |
| 09:40, далі щогодини до 20:40 | Нагадування про звітні дедлайни грантів у чат «Фінанси МикВісті» (FINANCE_CHAT_ID): пороги 7 і 2 доби; прогін повторюється щогодини, бо раз на добу означав би, що будь-який збій з'їдає цілий день мовчки — спрацьовує перший непройдений, тож пропущений день або відсутні права в чаті не гублять нагадування; подані й прийняті пропускаються, позначка ставиться лише після успішної відправки. У повідомленні — лінк на папку проєкту в Drive і кнопка «Дедлайни в апці» (лінк апки + startapp=reports; лінк бот визначає сам через get_me, налаштовувати нічого не треба) |
| 10:00 щодня | Перевірка правоохоронних органів |
| 13:00 щодня | Перевірка Gmail + правоохоронні органи |
| 16:00 щодня | Перевірка правоохоронних органів |
| 16:50 щодня | Перевірка Gmail |
| 15:00 щонеділі | Facebook тижневий звіт з AI (заодно знімок у social_stats) |
| 18:00 щонеділі | Instagram тижневий звіт з AI (заодно знімок у social_stats) |
| 18:30 щонеділі | Тихий тижневий зріз решти мереж у нору: Telegram (t.me), TikTok, YouTube, Viber — у них немає звіту в чат, на який можна присісти |
| Щогодини :00 | Тендери Prozorro |
| Кожні 3 год :15 (01,04,07,10,13,16,19,22) | Новини конкурентів (00:00–07:00 — у нічний буфер, дайджест о 07:15) |
| Щогодини :30 | Документи органів влади |
| Кожні 30 хв (10–18, пн–пт) | Перевірка мовчання каналу @nikvesti |
| Щогодини :05 і :35 | Детектор сплесків трафіку (GA4 Realtime, алерт при ≥2× від типового) |
| :10 і :40 (9–21) | Монітор білдера головної: >2 год без оновлення + ≥2 нові власні новини → нагадування |
| :20 (9–21) | Монітор миколаївських новин без Facebook: свіжі новини з region=1 (nodes) → перевірка ФБ (як /stat) → раз підказує редакції з чернеткою поста |
| 10:25 щодня | Прострочені creative tasks → картка Каті у «Сповіщеннях» апки (продовжити строк або «зарахувати скільки є»: із зарахованим — закрити done, без — dropped; «Зняти» лише в картці таска); dedup за парою «таск + дедлайн», тож продовжений і знову прострочений штовхне ще раз |
| Щогодини :45 | Звірка виконання creative tasks: свіжі проєктні публікації (7 днів) → суддя → авто-зарахування (high) чи черга Каті; плюс пости каналу за дисклеймерами проєктів (без AI) — теж у чергу. Тиха, AI лише за наявності кандидатів |
| Щогодини :50 | Інкрементальний sync дзеркала архіву (тихо пропускається без BOT_DATABASE_URL) |
| Щогодини :55 | Інкремент сутнісного шару: статті нори БЕЗ сутностей → витяг Haiku → entities/article_entities (тихий, опт-ін /entity_increment_on). Відбір за фактом відсутності зв'язків, не курсором, тож упалий витяг повертається наступної години (до 3 спроб, далі стаття визнається безнадійною); свіже береться першим |
| 11:20 щодня | Монітор місячних знімків виконання бюджету (mkrada «Щомісячна інформація»): нові xlsx → нора + анонс «Микита винюхав…» і розбір виконання по розпорядниках у канал «винюхав» (DOCUMENTS_CHAT_ID, не в редакцію); тихий, коли нового немає |
| Останній день місяця 19:00 | Місячний EN-звіт |
| 1-го числа 10:00 | Звіт про вартість AI за попередній місяць (в приват Олегу) |
| 1-го числа 10:30 | Місячний знімок аналітики (сайт GA4+SC, FB, IG, TG) у Google-таблицю «Аналітика МикВісті», короткий звіт Олегу в приват |

---

## Змінні середовища (Railway)

```
BOT_TOKEN
CHAT_ID = -1001857099475
PROZORRO_CHAT_ID = -1004322862192
DOCUMENTS_CHAT_ID = -1004322862192
FINANCE_CHAT_ID              # чат «Фінанси МикВісті» — нагадування про звітні дедлайни грантів; без неї модуль спить. Форму id вгадувати не треба: бот пробує і -100…, і просто мінус, і бере ту, що відгукнулась. Точний id підкаже /reports, викликаний у самому чаті
GA4_PROPERTY_ID = 321381722
GA4_CREDENTIALS              # JSON сервісного акаунту Google
GMAIL_USER
GMAIL_PASSWORD               # App Password
INSTAGRAM_TOKEN
INSTAGRAM_USER_ID = 17841400860799899
FACEBOOK_PAGE_TOKEN
FACEBOOK_PAGE_ID = 301719373180657
ANTHROPIC_API_KEY
OPENWEATHER_API_KEY
GOOGLE_KG_API_KEY            # опційно, простий API key (не сервісний акаунт) для /kg — Google Knowledge Graph Search API
YOUTUBE_OAUTH_CLIENT_ID      # опційно, OAuth client (Desktop app) для YouTube Analytics API — YouTube-блок таблиці аналітики (перегляди, години перегляду, підписники)
YOUTUBE_OAUTH_CLIENT_SECRET  # опційно, секрет того ж OAuth client
YOUTUBE_OAUTH_REFRESH_TOKEN  # опційно, refresh token від власника каналу (scope yt-analytics.readonly); як дістати — docstring handlers/youtube_analytics.py
TIKTOK_CLIENT_KEY            # опційно, TikTok Display API app (Login Kit) — TikTok-блок таблиці аналітики (підписники + вовлеченість по відео місяця; охоплення API не віддає)
TIKTOK_CLIENT_SECRET         # опційно, секрет того ж TikTok app
TIKTOK_REDIRECT_URI          # опційно, redirect URI, зареєстрований у пісочниці TikTok (напр. https://nikvesti.com/) — для /tiktok_auth
TIKTOK_REFRESH_TOKEN         # опційно, ручний сід refresh token (зазвичай не треба — /tiktok_auth сам кладе токен у storage.tiktok_oauth). TikTok ротує його, бот тримає актуальний. Налаштування — docstring handlers/tiktok_analytics.py
VIBER_AUTH_TOKEN             # опційно, токен супер-адміна Viber-каналу (Developer Tools каналу) — дзеркало Telegram→Viber (handlers/viber_mirror.py)
VIBER_WEBHOOK_URL            # опційно, HTTPS-endpoint, що віддає 200 (Viber вимагає заданий webhook перед постингом у канал); реєструється через /viber_setup
VIBER_SENDER_ID              # опційно, Viber-id, що жорстко закріплює автора постів (підпис під каналом); без нього — перший super admin каналу. Список id усіх учасників показує /viber_setup
VIBER_INVITE_URL             # опційно, публічний лінк запрошення каналу (invite.viber.com/?g2=…) — звідти парсяться підписники (numberOfWatchers); дефолт каналу МикВісті зашито в viber_mirror.py
WEBAPP_URL                   # опційно, https-домен сервісу на Railway — Mini App «Команда» (handlers/webapp.py); без нього + PORT (Generate Domain на Railway) веб-шар спить
WEBAPP_DIRECT_LINK           # опційно і зазвичай НЕ потрібно: лінк для груп бот збирає сам (https://t.me/<username>?startapp=…, Main Mini App). Ставити лише якщо апку зареєстровано окремо через /newapp і потрібна форма з short name (https://t.me/mykvisti_bot/team)
SPREADSHEET_ID = 1bsKzGRsQ7O1aa4TpxmzqEfIjRM1A0dso7zueYvCXB1I
SOCIAL_SPREADSHEET_ID        # опційно, таблиця «Аналітика МикВісті» (дефолт зашито: 1KNkxqN8ru4c2ez-x3nw9sEW-lXm562VdfJ3EEdbjGZk)
SOCIAL_SHEET_THEME           # опційно, тема таблиці аналітики: dark (дефолт) | light
ALLOWED_USER_IDS = 56631818,56424866,386403807   # Олег, Катя, Ліза — whitelist приватних повідомлень і NLQ
STATE_PATH                   # опційно, дефолт /data/prozorro_state.json
MISE_PYTHON_GITHUB_ATTESTATIONS = false

# БД бота (Postgres на Railway) — handlers/bot_db.py, дзеркало архіву + /dossier
BOT_DATABASE_URL             # референс на DATABASE_URL Postgres-плагіна (фолбек: DATABASE_URL)

# БД сайту (MySQL, read-only, SELECT only на nikvesti.*, SSL обов'язковий) — handlers/db.py
DB_HOST                      # 185.149.41.55
DB_PORT = 3306
DB_NAME = nikvesti
DB_USER                      # nikvesti_bot
DB_PASSWORD
DB_SSL_CA                    # опційно, шлях до CA сервера; без нього — SSL без перевірки cert
DB_CONNECT_TIMEOUT           # опційно, сек (дефолт 10)
DB_READ_TIMEOUT             # опційно, сек (дефолт 30)
```

**Доступ до БД сайту (KEY4):** whitelist за вихідними IP Railway; ліміти сервера —
5 одночасних з'єднань, 10000 запитів/год, 1000 з'єднань/год, 0 UPDATE. Модель у `db.py` —
з'єднання на запит (thread-safe), тільки читання. Бот стартує і без DB_* (модуль опційний).

**Railway Volume:** mount path `/data`.  
**Стан:** єдиний файл `/data/prozorro_state.json` для всіх модулів моніторингу.

---

## Storage — структура JSON

```json
{
  "document_ids": {
    "mayor_orders": ["51982", ...],
    "ova_orders": ["18165", ...],
    "oblrada_decisions": ["md5hash", ...],
    "prokuratura": ["424328", ...]
  },
  "competitor_ids": {
    "news_pn": ["345266", ...]
  },
  "tg_posts": {
    "320362": {"message_id": 82005}
  },
  "traffic_spikes": {
    "profile": {"2_14": [312, 298, ...]},
    "last_alert_at": "2026-07-02T14:35:00+03:00"
  },
  "builder_monitor": {
    "last_alert_at": 1783093933
  },
  "fb_missing": {
    "alerted": [321223, 321296],
    "baseline_done": true,
    "baseline_ver": 2
  },
  "news_search": {
    "56631818:56631818": {"items": [{"n": 1, "id": 320651, "date": "03.07.2026", "title": "...", "url": "..."}], "selected": [1, 3], "at": "2026-07-03T23:51:00"}
  },
  "ai_usage": {
    "2026-07": {"claude-sonnet-5": {"requests": 42, "input": 168000, "output": 9000, "cache_read": 140000, "cache_creation": 12000}}
  },
  "bot_usage": {
    "2026-07-21": {
      "56424866": {"name": "Катерина Середа (@sereda_ka)", "commands": {"stat": 2}, "nlq": 3, "questions": [{"q": "скільки трафіку за тиждень", "len": 26}], "tools": {"get_traffic_history": 2}, "backs": [{"topic": "Сєнкевич", "len": 8, "items": 3}]}
    }
  },
  "prozorro": { ... }
}
```

`None` (ключ відсутній) ≠ `[]` (ініціалізовано але порожньо). `None` = перший запуск, потрібен baseline.

**Обмеження росту (storage.py):** `competitor_ids` капляться на 1000/джерело (стрічка новин ~10-50 останніх, справжній bloat). `document_ids` — БЕЗ капу: сторінки проєктів рішень містять повну історію (тисячі записів), кап відрізав би її й старі документи щоразу виглядали б "новими" (був баг: спам за 2021). Ріст документів обмежений темпом публікацій. Тендери старші 120 днів прюняться разом з `message_to_tender` (у `bulk_save`). `tg_posts` — кап 20000. `traffic_spikes` — 8 замірів на слот. `bot_usage` — 30 днів, 40 питань і 20 беків на користувача на день. Запобіжник: `>DOCS_SANITY_MAX` (20) "нових" документів за прогон → ре-baseline без спаму + алерт (`/documents_rebaseline` — ручна чистка).

---

## Де живуть беклоги

**Єдиний список — [`docs/BACKLOG.md`](docs/BACKLOG.md)** (зведено 02.08). Нова ідея
йде туди, а не в модульний док. Решта файлів нижче — деталі й дослідження, з
яких беклог тягне рядки:

| Файл | Що там |
|---|---|
| [`docs/BACKLOG.md`](docs/BACKLOG.md) | **головний беклог**: усі напрямки, згруповані, з позначками стану (🔜 можна кодити · 🧭 потрібне рішення Олега · ⛔ заблоковано) |
| [`docs/PROJECT_CONTEXT.md`](docs/PROJECT_CONTEXT.md) | залишкові деталі, яким немає модульного дока (навмисна поведінка ранкового повідомлення, TG-id Сергія). Копію довідки й нумерований беклог звідси прибрано 02.08 — вони застаріли й брехали |
| [`docs/LONG_TERM_VISION.md`](docs/LONG_TERM_VISION.md) | стратегічні напрямки: підписки, власний сервер, AI-доступ до архіву. Нічого звідси не кодити без явного «кодь» |
| [`docs/MINI_APP_REDESIGN_PLAN.md`](docs/MINI_APP_REDESIGN_PLAN.md) | хвилі робіт і беклог Mini App «Команда» |
| [`docs/COMPENSATION_ADVISOR.md`](docs/COMPENSATION_ADVISOR.md) | порадник щодо зарплат і премій — задум із дослідженням, НЕ в розробці |
| [`docs/ARCHIVE_INTELLIGENCE.md`](docs/ARCHIVE_INTELLIGENCE.md), [`docs/ENTITY_LAYER_PLAN.md`](docs/ENTITY_LAYER_PLAN.md) | дорожня карта інституційної памʼяті й сутнісного шару |
| [`docs/ENTITY_MERGE_PLAN.md`](docs/ENTITY_MERGE_PLAN.md) | злиття дублів сутностей і зведення ролей до канону: кроки 1–2 (замір `/entity_scale` + дедуп ролей з кнопками) зроблені 02.08, детектор карток/черга/суддя чекають рішення Олега за числами заміру |
| [`docs/PROMISES_BANK.md`](docs/PROMISES_BANK.md) | банк тем: обіцянки, горизонти, нагадування редакції — бриф для окремої сесії, не кодилось |
| [`docs/IMPACT_ARCHIVE.md`](docs/IMPACT_ARCHIVE.md) | імпакт-архів: механіка, схема, беклог (медальки в профілі, PDF, добірки за донором) |
| локальні беклоги в доках модулів | дрібне по своєму модулю (`STAT_MODULE.md`, `NATURAL_LANGUAGE_QUERIES_MODULE.md`, `DOCUMENTS_MODULE.md`) |

**Правило:** велика ідея з дослідженням — окремий файл у `docs/` плюс рядок у
`BACKLOG.md`; дрібний фікс — просто рядок у `BACKLOG.md`. Нічого не заводити
повз нього, інакше список знову розповзеться по семи файлах.

## Детальна документація модулів

- [`docs/PROJECT_CONTEXT.md`](docs/PROJECT_CONTEXT.md) — загальний контекст, беклог
- [`docs/DOCUMENTS_MODULE.md`](docs/DOCUMENTS_MODULE.md) — моніторинг документів влади
- [`docs/LAW_ENFORCEMENT_MODULE.md`](docs/LAW_ENFORCEMENT_MODULE.md) — моніторинг правоохоронців
- [`docs/COMPETITORS_MODULE.md`](docs/COMPETITORS_MODULE.md) — моніторинг конкурентів
- Тендери Prozorro — окремого дока немає, детальний опис у док-стрінгу [`handlers/prozorro.py`](handlers/prozorro.py)
- [`docs/BUILDER_MONITOR_MODULE.md`](docs/BUILDER_MONITOR_MODULE.md) — монітор білдера головної + схема БД сайту (options/nodes/users/logs), розвідана 03.07
- [`docs/STAT_MODULE.md`](docs/STAT_MODULE.md) — статистика матеріалів (/stat)
- [`docs/ENGLISH_REPORT_MODULE.md`](docs/ENGLISH_REPORT_MODULE.md) — EN-звіт
- [`docs/FOX_LORE.md`](docs/FOX_LORE.md) — identity frame персонажа Лиса Микити
- [`docs/NATURAL_LANGUAGE_QUERIES_MODULE.md`](docs/NATURAL_LANGUAGE_QUERIES_MODULE.md) — природномовні запити (Agentic Query Layer): GA4 + Search Console, tool use, whitelist
- [`docs/ENERGY_OUTAGE_MODULE.md`](docs/ENERGY_OUTAGE_MODULE.md) — графік відключень: API off.energy.mk.ua, адресний каскад, CSV-експорт, наступні кроки
- [`docs/LONG_TERM_VISION.md`](docs/LONG_TERM_VISION.md) — довгострокові ідеї: підписки, перехід на власний сервер+MySQL, AI-доступ до 17-річного архіву новин
- [`docs/ARCHIVE_INTELLIGENCE.md`](docs/ARCHIVE_INTELLIGENCE.md) — стратегія інституційної пам'яті: рівні, вартість, дорожня карта, продукти
- [`docs/ARCHIVE_MIRROR_MODULE.md`](docs/ARCHIVE_MIRROR_MODULE.md) — дзеркало архіву в Postgres бота, FTS-пошук, /dossier (хвиля A)
- [`docs/ENTITY_LAYER_PLAN.md`](docs/ENTITY_LAYER_PLAN.md) — план Досьє v2 (індекс-файл, /dossier_deep) і сутнісного шару (хвиля D); бриф для нової сесії, «кодь» не давався
- [`docs/REVIEW_2026_07.md`](docs/REVIEW_2026_07.md) — ревізія липня 2026: апрувнутий план оптимізацій і розвитку (хвилі впровадження)
- [`docs/BUDGET_REVISIONS_MODULE.md`](docs/BUDGET_REVISIONS_MODULE.md) — бюджет Миколаєва: версії плану з «Порівняльних таблиць» рішень сесії, схема budget, парсер xlsx, валідація, дельти
- [`docs/SOCIAL_SHEET_MODULE.md`](docs/SOCIAL_SHEET_MODULE.md) — Google-таблиця аналітики: структура річного листа, джерела місячних метрик, формули/оформлення, план перенесення старої ручної таблиці
- [`docs/TEAM_APP_MODULE.md`](docs/TEAM_APP_MODULE.md) — Mini App «Команда»: creative tasks (точково і масово з проєкту), проєкти з тематиками/порядком/звітністю/Drive, KPI (норми на відділ + правки по людині, дашборд і помісячна динаміка), відділи Creative/Newsroom/Діджитал, два матчі ідентичності (Telegram→людина, людина→users.id із піном), судья-матчер зарахування виконання, повний список API і таблиць, налаштування (Railway домен + BotFather /newapp), дорожня карта

---

## Команда редакції (TEAM в ai_messages.py)

Повний список — джерело істини словник `TEAM` в `handlers/ai_messages.py` (звідти
ж fb_missing бере TG-хендл автора). Тримати таблицю синхронною з `TEAM`.
**Написання ПІБ має збігатися з `users` на сайті** — по ньому матчаться фото в
апці і факт KPI (тому «Кірілл Витвицький», а не «Кирил»); дублі акаунтів у
`users` лікуються піном `/kpi_link`. Відділи (Creative/Newsroom/Діджитал) —
не тут, а в `team_roster.ROSTER` + `team_dept` у Норі: Катя переносить людей
прямо в апці, без деплою.

| Хто | TG | Роль |
|---|---|---|
| Олег Деренюга | @derenuga | Керівник і візіонер МикВісті |
| Катерина Середа | @sereda_ka | Головна редакторка |
| Олена Бондаренко | (тег за id) | Фінансова менеджерка, звітність по грантах |
| Юлія Бойченко | @boichenko13 | Лід-журналістка, великі аналітичні матеріали |
| Аліса Мелікадамян | @lislislisalisa | Лід-журналістка і судова репортерка, випускова редакторка |
| Світлана Іванченко | @Lana_PRpolka | Редакторка стрічки новин |
| Альона Коханчук | @Aliona_Banu | Лід-журналістка, екологія і новини громад Миколаївщини |
| Аліна Квітко | @Aliniskv | Лід-журналістка, міськрада, підзвітність влади, локальна демократія |
| Марія Хаміцевич | (тег за id) | Редакторка стрічки новин, афіші «Куди піти в Миколаєві» |
| Сергій Овчаришин | (тег за id) | Оператор, фотограф, відеопродюсер |
| Єлизавета Москвіна | @mskvn1 | Керівниця соцмереж, діджіталу і дистрибуції |
| Іміра Борухова | @Imira_91 | Провідна SMM-спеціалістка, Instagram |
| Даріна Мельничук | @dariimlk | Журналістка — і в кадрі, і в матеріалах, молода зірка редакції |
| Юлія Лук'яненко | @Yuliia_Lukianenko | Досвідчена журналістка |
| Таміла Ксьонжик | @tamilissssa | Журналістка, прийшла на практику і залишилась |
| Кристина Леонова | @skxxlw | Журналістка, прийшла на практику і залишилась |
| Кірілл Витвицький | @simada24 | Помічник оператора і фотограф, учень Сергія |
| Ірина Федорович | @diiessa | Перекладачка, англійська версія сайту |
